{
  "name": "@tokenring-ai/browser-agent-storage",
  "description": "Provides browser-based agent state storage for the TokenRing ecosystem.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/checkpoint": "0.2.0",
    "zod": "catalog:"
  },
  "license": "MIT",
  "main": "index.ts",
  "type": "module",
  "devDependencies": {
    "vitest": "catalog:",
    "typescript": "catalog:"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  }
}
